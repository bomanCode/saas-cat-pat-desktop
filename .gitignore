# --- Node / Vite ---
node_modules/
dist/
dist-ssr/
*.local
.vite/

# --- Rust / Tauri ---
src-tauri/target/
src-tauri/gen/
src-tauri/Cargo.lock.bak

# --- Vercel / API ---
.vercel/
api/.env

# --- Env / secrets — never commit real values ---
.env
.env.*
!.env.example

# --- DB (local dev artifacts; schema lives in db/schema.sql, not the binary) ---
*.db
*.db-journal
*.sqlite
*.sqlite-journal

# --- OS / editor ---
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
.idea/

# --- Logs ---
*.log
npm-debug.log*
# Podman / Docker build artifacts
.podman/
.docker/
comnyang-dev.tar
